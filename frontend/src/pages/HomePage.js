import React from 'react';
import { Hero, TopDeals } from '../components';

const HomePage = () => {
  return (
    <main>
      <Hero />
      <TopDeals />
    </main>
  );
};

export default HomePage;
